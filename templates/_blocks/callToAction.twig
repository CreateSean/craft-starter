  <section class="py-24 border-t border-gray-200 bg-gray-100">
    <div class="container">
      <div class="flex flex-col justify-center items-center gap-8">
        {% if block.sectionTitle|length %}
          <h2>{{ block.sectionTitle }}</h2>
        {% endif %}
          {{ block.button.getLink({
            class: 'btn',
          }) }}
      </div><!-- /.flex flex-row -->
    </div><!-- /.container -->
  </section>
