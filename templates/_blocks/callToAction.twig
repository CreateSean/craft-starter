  <section class="bg-gray-100 border-t border-gray-200 {{ block.padding.value ?? 'section-y-padding' }}">
    <div class="container">
      <div class="flex flex-col items-center justify-center gap-8">
        {% if block.sectionTitle|length %}
          <h2>{{ block.sectionTitle }}</h2>
        {% endif %}
          {{ block.button.getLink({
            class: 'btn',
          }) }}
      </div><!-- /.flex flex-row -->
    </div><!-- /.container -->
  </section>
